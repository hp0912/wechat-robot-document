# 微信机器人使用文档

这是一个基于 VitePress 构建的使用文档项目，默认使用 `pnpm` 管理依赖，并提供 Docker 部署能力。

## 本地使用

```bash
pnpm install
pnpm dev
```

## 类型检查与构建

```bash
pnpm typecheck
pnpm build
```

## Docker 部署

```bash
docker compose up -d --build
```
